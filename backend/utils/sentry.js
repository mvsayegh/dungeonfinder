// import * as Sentry from "@sentry/node";

// // Verifique se o DSN está configurado corretamente
// if (!process.env.SENTRY_DSN) {
//   console.warn("Sentry DSN is not configured. Make sure to set SENTRY_DSN in your environment variables.");
// } else {
//   Sentry.init({ dsn: process.env.SENTRY_DSN });
// }

// export default Sentry;
